# Invitación de boda — v3.3 (Next.js + TS + Tailwind + Atomic)

- Hero totalmente responsive con **ResponsiveMedia** (aspect ratio por breakpoint y fallback si no hay imagen).
- Wrapper superior con **radial gradient fluido** sin alturas fijas.
- Navbar móvil con drawer, tipografías con `next/font` (Playfair Display + Inter).
- Countdown sin hydration mismatch (placeholder en SSR).
- Gallery, Timeline, secciones y formularios responsive.
- Mantiene Atomic Design y `sections/registry` para reordenar secciones.

## Correr
```bash
npm install
npm run dev
```


